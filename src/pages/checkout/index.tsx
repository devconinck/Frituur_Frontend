import { NextPage } from "next";

export const Checkout: NextPage = () => {
  return (
    <div className="mt-5 flex flex-col items-center text-2xl">
      <p>To see this page you must place an order!</p>
    </div>
  );
};

export default Checkout;
